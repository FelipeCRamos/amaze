#include "ia.hpp"

/*
A exemplo do pdf, teremos esta função.
Como não temos um labirinto, e sim uma cobra tentando chegar na maça,
usaremos como destino do labirinto a própria maça, e como posição
teremos a própria Cobra, mais especificamente a cabeça da cobra.
Deveremos usar a informação(o data) da posição da lista? Neste caso,
usando o std::pair como parâmetro das funções?
*/
/*
bool solve_maze( const Maze & mz, const Snake & snk ){
	if( mz.is_apple( snk.head ) ) return true;
	if( mz.is_marked( snk.head ) ) return false;

	//TODO...
		
}	
*/
